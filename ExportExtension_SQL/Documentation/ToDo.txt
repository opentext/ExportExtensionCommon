Errors:
	* 

ToDo
	* Nach fehlerhaftem Login nicht umschalten auf das nächste Tab
	* Unit-Tests für das ViewModel
	* Test gegen andere DBs

	* Alle SQL-Typen nach und nach implementieren
		- Static-Liste: Datentyp ent-Kommentieren
		- ColumnMetadata updaten (SqlClient_Test.cs) --> Ändert dbo.MyTable
		- Ggf. erweitern (wenn ein neuer .Net type verwendet wird)
			- ConvertStringToObject (SqlClient.cs)
			- compareSqlColumn (SqlClient_Test.cs)
			- Culture-Test (SqlClient_Test.cs)
